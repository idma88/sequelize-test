module.exports = (models) => {
  var modelObj = models.Accounts.Person;

  models.Accounts.Person.hasOne(
    models.Accounts.Location,
    {
      onDelete: 'CASCADE',
      foreignKey: 'accountId'
    }
  );
  models.Accounts.Person.hasOne(
    models.Accounts.Social,
    {
      onDelete: 'CASCADE',
      foreignKey: 'accountId'
    }
  );

//   class User {
//     static async New(accountId, message) {
//       if (accountId === undefined) return;
//       if ((message === undefined) || (message.length === 0)) return;

//       return modelObj.create({
//         accountId: accountId,
//         message: message
//       });
//     };

//     static async Get() {
//       return modelObj.findAll();
//     }

//     static async Delete(id) {
//       if (id === undefined) return;

//       return modelObj.update(
//         { deleted: true },
//         { where: { id: id } }
//       );
//     }

//     static async UnDelete(id) {
//       if (id === undefined) return;

//       return modelObj.update(
//         { deleted: false },
//         { where: { id: id } }
//       );
//     }
//   }

//   return User;
}